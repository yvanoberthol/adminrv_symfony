{% extends 'base.html.twig' %}
{% block title %}
    {{ 'user.formadd.nav_title'|trans }}
{% endblock %}

{% block navbar %}
    {% include 'commons/navbar.html.twig' %}
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1 class="grand-titre">{{ 'user.formadd.title:'|trans }}</h1>
            {% if userNameExist is defined and userNameExist == true %}
                <div class="text-center alert alert-danger font-weight-bold">
                    <span class="fa fa-times-circle"></span> {{ 'user.formadd.userNameExist'|trans }}
                </div>
            {% endif %}
            {#{%  if errors is defined and errors|length >0 %}
                {% for error in errors %}
                    <div class="text-center alert alert-danger font-weight-bold">
                        <span class="fa fa-times-circle"></span> {{ error }}
                    </div>
                {% endfor %}
            {% endif %}#}
            {{ form_start(form) }}
            {{ form_row(form.username) }}
            {{ form_row(form.password) }}
            {{ form_row(form.enabled) }}
            {{ form_row(form.allRoles) }}
            <div class="form-group">
                <button type="submit" class="btn" style="background-color: purple; color:white"><span
                            class="fa fa-plus-circle"></span> {{ 'user.formadd.form.btn_add'|trans }}
                </button>
                <a href="{{ path('users') }}">{{ 'user.formadd.back_list'|trans }}</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script type="text/javascript">
        $(document).ready(function () {
            $('.selectpicker').selectpicker();
        });
    </script>
{% endblock %}