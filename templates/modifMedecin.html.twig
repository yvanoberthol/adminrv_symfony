{% extends 'base.html.twig' %}
{% block title %}
    {{ 'medecin.formmodif.nav_title'|trans }}
{% endblock %}

{% block navbar %}
    {% include 'commons/navbar.html.twig' %}
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1 class="grand-titre">{{ 'medecin.formmodif.title'|trans }}</h1>
            {% if medecinNameExist is defined and medecinNameExist == true %}
                <div class="text-center alert alert-danger font-weight-bold">
                    <span class="fa fa-times-circle"></span> {{ 'medecin.formmodif.medecinNameExist'|trans }}
                </div>
            {% endif %}
            {{ form_start(form) }}
            {{ form_row(form.matricule) }}
            {{ form_row(form.nom) }}
            {{ form_row(form.prenom) }}
            {{ form_row(form.email) }}
            {{ form_row(form.telephone) }}
            {{ form_row(form.ville) }}
            {{ form_row(form.date_naissance) }}
            {{ form_row(form.specialites) }}
            <div class="form-group">
                <button type="submit" class="btn btn-info">
                    <span class="fa fa-edit"></span> {{ 'medecin.formmodif.form.btn_edit'|trans }}
                </button>
                <a href="{{ path('medecins') }}">{{ 'medecin.formmodif.back_list'|trans }}</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script type="text/javascript">
        $(document).ready(function () {
            $('.selectpicker').selectpicker();
        });
    </script>
{% endblock %}